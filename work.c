#include "work.h"
#include "variable.h"
#include "function.h"

void Work(void)
{
#if 0
	if(1 == LowBat || 1 == IsCharf)
	{
		g_ucKeyState = 0;
	}
	switch (g_ucKeyState)
	{
	case 0:
		IsLight = 0;
		ControlLevelLow();
		break;
	}

#endif
}
